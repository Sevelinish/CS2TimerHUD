namespace TimerHud.Localization;

public sealed class MessageCatalog
{
    public required string LanguageName { get; init; }
    public required string LanguageChanged { get; init; }
    public required string PlayerOnly { get; init; }
    public required string TimerEnabled { get; init; }
    public required string TimerDisabled { get; init; }
    public required string ToggleDisabledByServer { get; init; }
    public required string BindUsage { get; init; }
    public required string BindButtons { get; init; }
    public required string BindCurrent { get; init; }
    public required string BindMissing { get; init; }
    public required string BindUnknown { get; init; }
    public required string BindSet { get; init; }
    public required string BindCleared { get; init; }
    public required string BindCycle { get; init; }
    public required string RunStarted { get; init; }
    public required string RunPaused { get; init; }
    public required string RunOverwritten { get; init; }
}
